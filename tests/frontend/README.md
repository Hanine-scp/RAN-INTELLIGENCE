# Frontend tests

Unit tests run from this folder (Vitest). E2E tests will go in `e2e/` (Playwright, planned).

```powershell
cd tests/frontend
npm install
npm test
```

Tests import application code via alias `@/` → `frontend/`.
